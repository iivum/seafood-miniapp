spring:
  application:
    name: config-service
  cloud:
    config:
      # 不从其他配置中心读取，自己就是配置中心
      enabled: false
  data:
    mongodb:
      uri: ${SPRING_DATA_MONGODB_URI:mongodb://mongodb:27017/seafood}
      database: seafood
