spring:
  application:
    name: admin-ui
  main:
    allow-circular-references: true

server:
  port: 8084

eureka:
  client:
    service-url:
      defaultZone: ${EUREKA_URI:http://localhost:8761/eureka/}

vaadin:
  productionMode: false
  url-mapping: /admin/*
